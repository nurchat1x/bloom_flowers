# Bloom Flowers

Витрина цветочного бренда Bloom Flowers (Шымкент).

## Запуск

```bash
npm install
npm run dev
```

## Важно

Номер WhatsApp задаётся в `src/data/contact.ts` (`WHATSAPP_NUMBER` — цифры с кодом страны, без `+`).
